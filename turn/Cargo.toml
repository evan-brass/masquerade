[package]
name = "turn"
version = "0.1.0"
edition = "2024"

[dependencies]
md5 = "0.7.0"
rand = "0.9.1"
smoltcp = { version = "0.12.0", default-features = false, features = ["proto-ipv6"] }
stun = { path = "../stun" }
