[Unit]
Description=Pseudo-VPN gateway speaking the TURN wire format
After=relay-isolation.service

[Install]
WantedBy=multi-user.target

[Service]
Restart=always
Environment=RUST_LOG="trace"
ExecStart=/usr/sbin/polo -i turn
